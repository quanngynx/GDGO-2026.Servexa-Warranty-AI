export enum PermissionResource {
  USERS = 'users',
  INVENTORY = 'inventory',
  WAREHOUSE = 'warehouse',
  ANALYTICS = 'analytics',
  ADMIN = 'admin',
  SYSTEM = 'system',
  NOTIFICATIONS = 'notifications',
  BROADCASTS = 'broadcasts',
}
