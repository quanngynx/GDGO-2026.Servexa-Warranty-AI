name: Production Readiness Governance

on:
  pull_request:
    paths:
      - ".github/workflows/production-readiness.yml"
      - "documents/adr/ADR-012-production-readiness-hard-gates.md"
      - "documents/production-readiness/**"
      - "scripts/validate-p0-gate.mjs"
      - "scripts/validate-p0a-gate.mjs"
      - "documents/adr/ADR-013-p0a-synthetic-readiness-boundary.md"
      - "docker-compose.p0a.yml"
      - "package.json"
  push:
    branches:
      - main
    paths:
      - ".github/workflows/production-readiness.yml"
      - "documents/adr/ADR-012-production-readiness-hard-gates.md"
      - "documents/production-readiness/**"
      - "scripts/validate-p0-gate.mjs"
      - "scripts/validate-p0a-gate.mjs"
      - "documents/adr/ADR-013-p0a-synthetic-readiness-boundary.md"
      - "docker-compose.p0a.yml"
      - "package.json"

jobs:
  p0-manifest:
    name: Validate P0 evidence manifest
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Validate P0 structure and gate consistency
        run: node scripts/validate-p0-gate.mjs

      - name: Validate P0A structure and preserve P0B block
        run: node scripts/validate-p0a-gate.mjs